# Core TTS Dependencies
torch>=2.0.0
torchaudio>=2.0.0
numpy>=1.24.0  # Compatible with Python 3.12

# Kokoro TTS Engine
# Note: Current kokoro-tts package has different API than expected by this codebase
# The codebase expects hexgrad/Kokoro-82M which may need manual installation
# kokoro-tts  # Install manually if needed

# Web Framework and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0

# Audio Processing
soundfile>=0.12.0
librosa>=0.10.0

# Text Processing and Document Parsing
pypdf>=3.17.0
pymupdf>=1.23.0  # fitz - alternative PDF reader
ebooklib>=0.18
beautifulsoup4>=4.12.0

# System Monitoring and Utilities
psutil>=5.9.0

# CosyVoice2 Dependencies (optional but recommended)
modelscope>=1.11.0
conformer==0.3.2
hydra-core>=1.3.0
lightning>=2.0.0
phonemizer>=3.2.0
diffusers>=0.29.0
transformers>=4.51.0
omegaconf>=2.3.0
onnx>=1.16.0
onnxruntime>=1.18.0
matplotlib>=3.7.0  # Required for CosyVoice2
gradio>=5.4.0  # Web interface components

# Montreal Forced Alignment (optional - install manually if needed)
# montreal-forced-alignment  # Comment: Install via conda if needed: conda install -c conda-forge montreal-forced-alignment

# Text Processing
nltk>=3.8

# Machine Learning and AI
peft>=0.17.0  # Fixes LoRA deprecation warnings

# Additional Dependencies
wget>=3.2  # Required for CosyVoice2 model downloads
pyarrow>=18.0.0  # Required for CosyVoice2 dataset processing

# Note: Some packages may be auto-installed with the TTS engines
# whisper dependency is handled by openai-whisper to avoid conflicts
openai-whisper>=20231117